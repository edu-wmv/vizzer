# class Error():
#   def __init__(self, op):
#     self.op = op
  
#   def urlNotFound():
#     self.op('/project1/out_text').text = 'URL not found' # type: ignore
#     raise Exception('URL not found')

#   def mediaUserTokenNotFound():
#     self.op('/project1/out_text').text = 'Media User Token not found' # type: ignore
#     raise Exception('Media User Token not found')

#   def urlNotValid(reason: str):
#     self.op('/project1/out_text').text = 'URL not valid ' + reason # type: ignore
#     raise Exception('URL not valid ' + reason)

#   def songNotFound():
#     self.op('/project1/out_text').text = 'Song not found' # type: ignore
#     raise Exception('Song not found')

#   def failedDecryption():
#     self.op('/project1/out_text').text = 'Failed decryption' # type: ignore
#     raise Exception('Failed decryption')